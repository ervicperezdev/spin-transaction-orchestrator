{{- if .Values.paymentProviderMock.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "transaction-api.fullname" . }}-provider-mock
data:
  execute-approved.json: |
    {"request":{"method":"POST","urlPath":"/provider/v1/execute"},"response":{"status":200,"headers":{"Content-Type":"application/json"},"jsonBody":{"status":"APPROVED","transactionId":"mock-transaction","balance":974.50,"executedAt":"2026-01-01T00:00:00Z"}}}
  execute-rejected.json: |
    {"priority":1,"request":{"method":"POST","urlPath":"/provider/v1/execute","bodyPatterns":[{"matchesJsonPath":"$[?(@.accountId == 'acct-provider-rejected')]"}]},"response":{"status":200,"headers":{"Content-Type":"application/json"},"jsonBody":{"status":"REJECTED","transactionId":"mock-rejected-transaction","rejectionCode":"INSUFFICIENT_FUNDS","rejectionReason":"Insufficient funds"}}}
  execute-provider-error.json: |
    {"priority":1,"request":{"method":"POST","urlPath":"/provider/v1/execute","bodyPatterns":[{"matchesJsonPath":"$[?(@.accountId == 'acct-provider-error')]"}]},"response":{"status":503}}
  execute-timeout.json: |
    {"priority":1,"request":{"method":"POST","urlPath":"/provider/v1/execute","bodyPatterns":[{"matchesJsonPath":"$[?(@.accountId == 'acct-provider-timeout')]"}]},"response":{"status":200,"fixedDelayMilliseconds":4000,"headers":{"Content-Type":"application/json"},"jsonBody":{"status":"APPROVED","transactionId":"late","balance":1.00}}}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "transaction-api.fullname" . }}-provider-mock
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "transaction-api.name" . }}-provider-mock
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "transaction-api.fullname" . }}-provider-mock
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "transaction-api.name" . }}-provider-mock
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "transaction-api.name" . }}-provider-mock
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: wiremock
          image: {{ .Values.paymentProviderMock.image | quote }}
          ports: [{ name: http, containerPort: 8080 }]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          volumeMounts:
            - { name: mappings, mountPath: /home/wiremock/mappings, readOnly: true }
            - { name: tmp, mountPath: /tmp }
      volumes:
        - name: mappings
          configMap: { name: {{ include "transaction-api.fullname" . }}-provider-mock }
        - name: tmp
          emptyDir: {}
{{- end }}
