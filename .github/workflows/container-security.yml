name: Container security

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  hadolint:
    name: Lint Dockerfile
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Run Hadolint policy
        run: docker run --rm -i hadolint/hadolint:v2.12.0 < Dockerfile

  trivy:
    name: Scan container image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build image for scanning
        run: docker build --tag spin-transaction-orchestrator:${{ github.sha }} .
      - name: Export image without registry credentials
        run: docker save --output image.tar spin-transaction-orchestrator:${{ github.sha }}
      - name: Scan vulnerabilities and embedded secrets
        run: >-
          docker run --rm
          --volume "$GITHUB_WORKSPACE:/workspace:ro"
          aquasec/trivy:0.58.1 image
          --input /workspace/image.tar
          --scanners vuln,secret
          --severity HIGH,CRITICAL
          --ignore-unfixed
          --exit-code 1
          --no-progress
      - name: Generate SBOM
        run: >-
          docker run --rm
          --volume "$GITHUB_WORKSPACE:/workspace"
          anchore/syft:v1.18.1
          docker-archive:/workspace/image.tar
          -o spdx-json=/workspace/sbom.spdx.json
      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ github.sha }}
          path: sbom.spdx.json
          retention-days: 30
